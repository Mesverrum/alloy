# Network SNMP (this fork)

See **[docs/network-snmp.md](docs/network-snmp.md)**.

Branch `network-snmp`:

- Curated snmp_exporter modules baked into `Dockerfile.network`
- Prometheus-shaped `snmp-discovery` binary (named `auth` + SuperQ fingerprinters → `module`)

Lab harness: [network-o11y-demo](https://github.com/Mesverrum/network-o11y-demo) `docs/alloy-network-fork.md`.
